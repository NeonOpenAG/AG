{# This inherrites from /app/Resources/views/base.html.twig #}
{% extends 'base.html.twig' %}

{% block title %}Delete{% endblock %}

{% block h1 %}Delete{% endblock %}

{% block body %}

    <h2>Delete file</h2>

    Are you sure you want to dlete the following files from the OAG Toolkit.
    <div>
        <ul>
            {% for id, file in files %}
            <li>{{ file }}</li>
            {% endfor %}
        </ul>
    </div>

            <a href='{{ path('oagfile_delete', { 'ids': ids }) }}'>DELETE</a> <a href='{{ path('app_index') }}'>Cancel</a>

{% endblock %}